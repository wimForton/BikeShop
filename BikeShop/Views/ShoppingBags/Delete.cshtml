@model BikeShop.Models.ShoppingBagModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ShoppingBag</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Date)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Date)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="@Model.IdentityUserId" /><!--form wordt opgevuld met parameters-->
        <input type="hidden" asp-for="@Model.IdentityUserId" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <!--<a asp-action="Index">Back to List</a>-->
    </form>
    <a class="btn btn-primary" asp-controller="Account" asp-action="Details" asp-route-id="@Model.IdentityUserId">Cancel</a>
</div>
